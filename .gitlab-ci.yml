build:
    stage: build
    script:
        - apt-get update
        - apt-get install -y build-essential automake autoconf libpng-dev libjpeg-dev git
        - bash autogen.sh
        - ./configure
        - make

